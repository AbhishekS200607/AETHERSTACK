 Here is the updated Tiered Rewards & Commission Structure:

  ┌──────────────┬───────────┬──────────────────┬────────────────────────────────────┐
  │ Milestone    │ Tier Name │ Commission Range │ Physical Reward                    │
  ├──────────────┼───────────┼──────────────────┼────────────────────────────────────┤
  │ 10 Projects  │ Bronze    │ 7% — 10%         │ Official Aetherstack T-Shirt       │
  │ 25 Projects  │ Silver    │ 10% — 12%        │ Elite Hoodie + Branded Mug         │
  │ 50 Projects  │ Gold      │ 12% — 15%        │ Desk Plaque + Tech Kit (Powerbank) │
  │ 100 Projects │ Platinum  │ 15% — 20%        │ Premium Backpack + Team Dinner     │
  └──────────────┴───────────┴──────────────────┴────────────────────────────────────┘
  ---

  The Plan to Apply This:

  1. Database Update
  We will add 4 specific columns to track each claim separately:

   1 ALTER TABLE public.marketers
   2 ADD COLUMN IF NOT EXISTS claimed_tier1 BOOLEAN NOT NULL DEFAULT FALSE,
   3 ADD COLUMN IF NOT EXISTS claimed_tier2 BOOLEAN NOT NULL DEFAULT FALSE,
   4 ADD COLUMN IF NOT EXISTS claimed_tier3 BOOLEAN NOT NULL DEFAULT FALSE,
   5 ADD COLUMN IF NOT EXISTS claimed_tier4 BOOLEAN NOT NULL DEFAULT FALSE;

  2. API Update (api/marketing.js)
   * Modify /claim-reward to accept a tier (1, 2, 3, or 4).
   * The logic will check if the user has enough projects for that specific tier before allowing the claim.
   * Emails will be updated to mention exactly which reward was claimed.

  3. Marketer Dashboard (marketing-dashboard.html)
   * We will replace the single reward card with 4 sleek Milestone Cards.
   * Each card will show the Commission Range (e.g., "Earn 12-15% per project").
   * Each card will have its own progress bar and "Claim" button.

  4. Admin Panel (admin.html)
   * The "Reward" column will be expanded to show 4 checkboxes (T1, T2, T3, T4) so you can track which ones you have shipped to the marketer.

  Shall I proceed with applying these changes? (I will start with the SQL and then update the code).